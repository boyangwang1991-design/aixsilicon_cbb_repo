# release/manifest.yaml — popcount 发布候选清单（G8）
# status: candidate —— 仅 Workflow Gate 可确认为 released（artifact-contract §5）

status: candidate
version: 0.1.0
vlnv: aixsilicon:cbb:popcount:0.1.0
release_date: 2026-08-27
maturity: E2
license: Apache-2.0

artifacts:
  rtl:
    - rtl/popcount.sv
    - rtl/impl/impl_tree/popcount.sv
    - rtl/impl/impl_column_compress/popcount.sv
    - rtl/impl/impl_lookup/popcount.sv
  fusesoc_core: fusesoc/aixsilicon_cbb_popcount.core
  contract:
    - cbb.yaml
    - behavior.yaml
    - profiles.yaml
  verification:
    - verification/simulation/popcount_tb.sv
    - verification/scripts/run_static_checks.sh
    - verification/scripts/run_functional_sim.sh
    - trace/rtm.yaml

sbom:
  runtime_dependencies: []          # A1 原子构件，零嵌套 CBB 依赖
  verification_dependencies: []     # TB/SVA 随包自足，无外部 VIP/dv_common 依赖
  license_finding: 无第三方引入代码

evidence_refs:
  static_baseline: evidence/g3_static/
  functional: evidence/g4_functional/
  ppa: evidence/ppa/run-20260827-01/
  rtm: trace/rtm.yaml
  gates: reports/quality/gates/popcount.yaml   # 相对 cbb_repo 根

sha256: null                        # Workflow Gate 确认发布时固化（工具生成）

known_limitations_ref: docs/qualification-report.md#2-已知限制
