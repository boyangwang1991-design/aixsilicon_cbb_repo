# SAF-005 需求接入与变更计划

用户授权依据：本会话要求按 SAF-005-CONTRACT.MD 实现 CBB，并记录 skill 优化与待改进点。

- 范围：一个 A2 CBB 工程包、SAF-005 登记信息及派生索引，以及私有 skill 复盘。
- 消费者：合约列举的 CPU、NPU、DMA、安全 FSM 的同步 Main/Shadow 观测通道。
- 风险：功能安全高风险构件；仿真、综合不能替代物理独立性与系统安全签核。
- 命名：资产名与目录保留 `lockstep_comparator`，RTL 顶层采用合约名 `lockstep_comparator_logic`。
- 复用判断：SAF-003/004 仅为 planned，缺少可复用实现；本例固定延迟与比较逻辑按合约第 33 节保留为内部结构，不新增子依赖。
- HWIF：使用标量/向量观测端口，无总线协议，因此无 HWIF 依赖。
- 执行顺序：规格与严格检查 → 架构/详设 → RTL/SVA → 参数空间回归 → 综合表征 → 候选交付。

当前授权覆盖本地实现和验证，不包含发布；成熟度依据门禁证据处理。
