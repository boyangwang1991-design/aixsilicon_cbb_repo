# Release Manifest（G8 候选）— width_conversion_fifo
# 状态：candidate（只有 Workflow Gate G8 可确认 released）
# 版本：0.1.0（SemVer）

schema_version: 1.0
release:
  cbb: aixsilicon:cbb:width_conversion_fifo
  version: 0.1.0
  vlnv: aixsilicon:cbb:width_conversion_fifo:0.1.0
  fusesoc_core: aixsilicon_cbb_width_conversion_fifo.core
  status: candidate
  date: "2026-08-26"
  maturity: E2              # Implemented + Verified（E3 需 PPA/消费者证据）

artifacts:
  - path: rtl/interface/width_conversion_fifo_pkg.svh
    hash: sha256:待发布时固化
  - path: rtl/impl/impl_pointer_count/width_conversion_fifo.sv
    hash: sha256:待发布时固化
  - path: fusesoc/aixsilicon_cbb_width_conversion_fifo.core
    hash: sha256:待发布时固化
  - path: evidence/g3_static/
    hash: sha256:待发布时固化
  - path: evidence/g4_functional/
    hash: sha256:待发布时固化

sbom:
  license: Apache-2.0
  dependencies: []

known_limits:                # 与 qualification-report.md §5 一致
  - 仅整数比宽度转换；非整比 gearbox 不支持
  - 单时钟同步复位；无 CDC
  - DEPTH >= RATIO 强制（PC-005）
  - 宽侧位宽上限 4096 bit（PC-004）

release_notes:
  - "v0.1.0 (candidate)：单时钟同步 FIFO + 整数比宽度转换（A2/A3）。G0-G5 全通过，G6 PPA 受库缺失阻断（E0）。经 VCS 仿真（N2W/W2N）、SVA 断言、变异测试、SpyGlass lint 验证。"