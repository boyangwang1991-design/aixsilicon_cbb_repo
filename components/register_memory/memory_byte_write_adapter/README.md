# memory_byte_write_adapter

Memory Byte-write Adapter （A2, P1）

见 registry.yaml（SSOT）。CBB 工程包规范见 cbb-development-suite。

需求入口：[memory_byte_write_adapter 需求合同](memory_byte_write_adapter_contract.md)。文档齐全不代表实现或 Gate 通过。
