#!/usr/bin/env bash
# G4 功能仿真（定向+随机+SVA/变异）— 按需填充
# 纪律：原生仿真器可用则直接运行（VCS 在 build/eda/ 下）；证据以 *.txt 落盘
#       build/eda/evidence/g4_functional/（不入库）。
set -euo pipefail
echo "TODO: 填充 G4 仿真脚本（verify-cbb SKILL.md step 4）"
exit 10
