# adder_graph 详设

有界枚举小因子 (2^k±1)，公共子表达式复用；以节点位宽、深度、扇出及寄存器成本排序，不声明全局最优。

输入节点、常量零、固定移位、加减、乘法基线、寄存器和量化节点组成可校验 DAG。每个边记录精确系数、位宽与流水级。

## PPA 优化点

位宽决定加法面积，依赖深度决定组合路径下界；宽输入及稠密系数可能提供结构优化空间。输入 fanout、配平触发器、舍入/饱和比较器会抵消节点数收益。综合器可能把不同候选重构为相同网表；保留常规优化，无 dont_touch。面积/时序 Pareto 需同工艺实测，不由静态节点数直接推定。
