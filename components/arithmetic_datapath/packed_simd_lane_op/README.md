# packed_simd_lane_op

Packed SIMD Lane Operator （A2, P3）

见 registry.yaml（SSOT）。CBB 工程包规范见 cbb-development-suite。

需求入口：[packed_simd_lane_op 需求合同](packed_simd_lane_op_contract.md)。文档齐全不代表实现或 Gate 通过。
